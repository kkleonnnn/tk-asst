# 敏感信息绝不入库（本仓库 PUBLIC 公开，尤其严格）
.env
.env.*
*.env
*.key
*.pem
secrets/
private/
*credential*
*password*
*token*
*secret*

# 真实店铺导出的原始数据（如需分析，先脱敏后再放进仓库）
data/raw/
*.csv.real

# 系统/编辑器杂物
.DS_Store
Thumbs.db
.vscode/
.idea/
node_modules/

# inbox 原始素材（任何类型、任何层级）都不入库：
# 工作流是「原始素材留本地 → Claude 消化进 reference/ 文本 → 只提交文本」。
# 仅保留 inbox 里的 .md 笔记与 README（方便两人协作消化）。
inbox/**
!inbox/**/
!inbox/**/*.md
# 兜底：即便在 inbox，名字含敏感词的 .md 也不入库（覆盖上一行的 .md 放行；公开仓库尤其重要）
inbox/**/*credential*
inbox/**/*password*
inbox/**/*token*
inbox/**/*secret*
inbox/**/*.env.*
